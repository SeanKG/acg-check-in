import React from 'react';
import SvgIcon from 'react-native-svg-icon';
import svgs from '../icons/LogoTextFull';

const LogoIcon = (props) => <SvgIcon {...props} svgs={svgs} />;

export default LogoIcon;